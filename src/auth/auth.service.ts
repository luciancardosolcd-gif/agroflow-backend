import{Injectable,UnauthorizedException}from"@nestjs/common";import{JwtService}from"@nestjs/jwt";import{InjectRepository}from"@nestjs/typeorm";import{Repository}from"typeorm";import*as bcrypt from"bcryptjs";import{User}from"../users/user.entity";@Injectable()export class AuthService{constructor(@InjectRepository(User)private usersRepo:Repository<User>,private jwtService:JwtService){}async login(email:string,senha:string){const user=await this.usersRepo.findOne({where:{email:email.toLowerCase()}});if(!user||!(await bcrypt.compare(senha,user.senhaHash)))throw new UnauthorizedException("E-mail ou senha incorretos");if(user.status!=="ativo")throw new UnauthorizedException("Usuário inativo");await this.usersRepo.update(user.id,{ultimoAcesso:new Date()});const payload={sub:user.id,email:user.email,perfil:user.perfil};return{accessToken:this.jwtService.sign(payload,{expiresIn:"8h"}),refreshToken:this.jwtService.sign(payload,{expiresIn:"7d"}),user:{id:user.id,nome:user.nome,email:user.email,perfil:user.perfil}};}async refresh(token:string){try{const payload=this.jwtService.verify(token);return{accessToken:this.jwtService.sign({sub:payload.sub,email:payload.email,perfil:payload.perfil},{expiresIn:"8h"})}}catch{throw new UnauthorizedException("Token inválido")}}}